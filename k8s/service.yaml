apiVersion: v1
kind: Service
metadata:
  name: travel-agency-service
  namespace: default
spec:
  type: LoadBalancer
  selector:
    app: travel-agency
  ports:
    - protocol: TCP
      port: 80
      targetPort: 3000
